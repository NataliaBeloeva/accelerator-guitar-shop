enum AppRoute {
  Main = '/',
  Guitars = '/guitars',
}

enum ApiRoute {
  Guitars = '/guitars',
}

enum ServiceMessage {
  ServerFail = 'Сервер не отвечает, попробуйте позже',
}

export {AppRoute, ApiRoute, ServiceMessage};
